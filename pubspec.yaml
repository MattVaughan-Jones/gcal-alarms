name: meetingguard
description: Meeting Guard — detect upcoming calendar events and create alarms for them.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.6.4
  build_runner: ^2.13.1

flutter:
  uses-material-design: true
